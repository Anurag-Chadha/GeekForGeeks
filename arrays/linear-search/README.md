Check if an element x is present in the array,if present return the index of its first occurence,if not return -1.

**Approach:** Traverse the array till the element is found and return the index.If not found return -1.

**Time Complexity:** O(n)

**Space Complexity:** O(1)
