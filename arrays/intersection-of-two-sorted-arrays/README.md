To find intersection of two sorted arrays and return array having the intersection elements in sorted,unique order.

**Approach:** Two pointer approach.Moving both pointers according to requirment of question and inserting elements in the intersection array if not already inserted.

**Time Complexity:** O(n+m)

**Space Complexity:** O(1)
